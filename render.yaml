services:
  - type: web
    name: road-sign-detection-api
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: MODEL_URL
        sync: false
      - key: DEFAULT_IMAGE_SIZE
        value: "640"
      - key: OMP_NUM_THREADS
        value: "1"
